{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
{# <h1>User</h1>
<table class="table">
<tbody>
<tr>
<th>Id</th>
<td>{{ user.id }}</td>
</tr>
<tr>
<th>Email</th>
<td>{{ user.email }}</td>
</tr>
<tr>
<th>Roles</th>
<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
</tr>
<tr>
<th>Password</th>
<td>{{ user.password }}</td>
</tr>
<tr>
<th>Pseudo</th>
<td>{{ user.Pseudo }}</td>
</tr>
</tbody>
</table> #}
<div class="card mb-3" style="max-width: 540px;">
    <div class="row g-0">
        
            <div class="col-md-4">
                <img src="{{ vich_uploader_asset(user, 'imageFile') }}" class="img-fluid rounded-start" alt="{{ user.Pseudo }}">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                <h5 class="card-pseudo">{{ user.Pseudo }}</h5>
                <p class="card-mail">{{ user.email }}</p>
                <p class="card-collections">Liste des collections</p>
                <ul>
                    <li>Collections 1</li>
                    <li>Collections 2</li>
                    <li>Collections 3</li>
                </ul>
                </div>
            </div>
        
    </div>
</div>

    <a href="{{ path('user_index') }}" class="btn btn-warning"><i class="bi bi-house"> Retour à l'accueil</i></a>

    <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-info"><i class="bi bi-pencil"> Editer</i></a>
    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
