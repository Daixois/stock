{% extends 'base.html.twig' %}

{% block title %}Hello Tom{% endblock %}

{% block body %}
    {# <div class="card-body">
    {% for movie in movie %}
        <h5 class="card-title" style="width: 18rem">{{ movie.title}}</h5>
        <ul class="list-movie list-movie-flush">
            <li class="list-movie-name">Titre : {{movie.title}}</li>
            <li class="list-movie-name">Format : {{movie.sort}}</li>
        </ul>

    {% endfor %} #}

    
    <div class="container">
        <div class="row">
         {% for movie in movie %}
            <div class="card col-3" style="width: 18rem;">
                <img src="..." class="card-img-top" alt={{movie.title}}>
                    <div class="card-body">
                        <h5 class="card-title">{{ movie.title}}</h5>
                        <ul class="list-movie list-movie-flush">
                            <li class="list-movie-name">Titre : {{movie.title}}</li>
                            <li class="list-movie-name">Format : {{movie.sort}}</li>
                        </ul>
                            {# <a href="#" class="btn btn-primary">Go somewhere</a> #}
        
                    </div>
            </div>
            {% endfor %}
        </div> 
    </div>
    </div>
{% endblock %}
