
	<div class="card col-3">

		<img src="https://image.tmdb.org/t/p/w500/{{ movie.poster_path }}" class="card-img-top" alt="...">

		<div class="card-body">
			<h5 class="card-title">{{ movie.title }}</h5>
			<p class="card-text">{{ movie.overview }}</p>
		</div>

		<ul class="list-group list-group-flush">
			<li class="list-group-item">{{ movie.title }}</li>
		{# {% for genre in movie.genres %}
			<li class="list-group-item">{{ genre }}</li>
		{% endfor %} #}
			<li class="list-group-item">{{ movie.id }}</li>
		</ul>

		<div class="card-body">
			<a href="https://www.themoviedb.org/movie/{{ movie.id }}?language=fr" class="card-link">La Fiche Tmdb</a>
		</div>

			<div class="card-body">
				<a href="{{ path('home_searchid', {id: movie.id}) }}" class="card-link">Voir la fiche du film</a>
			</div>

		{% if addLink is defined %}
			<div class="card-body">
				<a class="btn btn-primary" href="{{ path('home_addid', {id: movie.id}) }}" class="card-link">Ajouter à la collection</a>
			</div>
		{% endif %}

	</div>
