{{ form_start(form, {attr: {class: 'filter'}}) }}
   {{ form_row(form.q)}}

   <h4 class="mt-2">Genres</h4>
    {{ form_row(form.genres)}}
    {# A venir #}
    {# <h4>Actors</h4>
    {{ form_row(form.actors)}} #}
    <h4 class="mt-2">Recherche par années</h4>
        <div class="row mt-2">
            <div class="col-md-6">
                {{ form_row(form.anneeMin)}}
            </div>
            <div class="col-md-6">
                {{ form_row(form.anneeMax)}}
            </div>
        </div>

    
    <button type="submit" class="btn btn-primary w-100 mt-2">Filtrer</button>


    
{{ form_end(form) }}

