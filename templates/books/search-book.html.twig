{% extends 'base.html.twig' %}

{% block title %}Hello Tom{% endblock %}

{% block body %}
	<div class ="addBook">
        {# <form id="researchForm" action="{{ path("books_searchBook")}}" method="get"> #}
		<form id="researchForm" action="..." method="get">
            <div class="mb-4">
                <input type="search" class="form-control" name="researchBook" placeholder="Recherche un autre livre à ajouter à ta collection">
            </div>
            <button type="submit" class="btn btn-primary mb-2" value="Search">Recherche</button>
        </form>
    </div>
	<div class="container">
			<div class="grid">
				{% for books in data %}
					{% include "books/_card-book.html.twig"%}
				{% endfor %}
			</div>
	</div>

{% endblock %}